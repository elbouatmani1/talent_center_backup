from django.apps import AppConfig


class SrfConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.srf'
    label = 'srf'
    verbose_name = 'SRF — Financial Follow-up'
