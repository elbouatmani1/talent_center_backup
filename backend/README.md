# Backend Area 🐍

This directory is reserved for the Python backend application. 
We will be using the **Django** framework to build the API and business logic for the PFE Talent Center.

*(The Django application has not been initialized yet. Stay tuned!)*
