# Shared Module

The `shared/` directory contains cross-feature code that is used throughout the entire React application.

This includes:
- Global UI components (e.g., Buttons, Inputs, Layouts)
- Generic formatting utilities
- Global styles and theme configurations
- Shared custom hooks
