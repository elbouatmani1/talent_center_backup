# Auth Assets

This directory contains static assets specific to the authentication feature.

- `/images`: Contains background images, illustrations, or graphics used in the auth flow.
- `/icons`: Contains explicit SVG or image files representing icons.
