/** Layout responsive — Interview Simulator (pleine page, bord à bord). */

/** Remplit tout le main (sans padding layout) — gauche, droite, bas. */
export const INTERVIEW_SIMULATOR_VIEWPORT_SHELL =
  'box-border flex h-full min-h-0 w-full max-w-none flex-col overflow-hidden bg-white font-inter';

export const INTERVIEW_SIMULATOR_PAGE_ROOT =
  'flex h-full min-h-0 w-full min-w-0 max-w-none flex-col overflow-hidden';
